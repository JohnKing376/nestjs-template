model User {
    id String @id @default(cuid())
    email String @unique
    firstName String?
    lastName String?

    //metadata
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}